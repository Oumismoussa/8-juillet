{% extends 'base.html.twig' %}

{% block title %} Inscription {% endblock %}

{% block body %}
    <h1>Register</h1>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}

    <fieldset>
    
    <legend> Mon identité </legend>
     {{ form_row(registrationForm.email) }}
     {{ form_row(registrationForm.lastname) }}
     {{ form_row(registrationForm.firstname) }}
 
    </fieldset>

    <fieldset>
    
    <legend> Requis </legend>
     {{ form_row(registrationForm.teacher) }}
     {{ form_row(registrationForm.token_mail) }}
     {{ form_row(registrationForm.hashtag) }}
     {{ form_row(registrationForm.abonnement_id) }}
     {{ form_row(registrationForm.etablissement_id ) }}
    {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
    </fieldset>
       
      
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
{% endblock %}
